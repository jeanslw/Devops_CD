# 2026-07-23

- 部署记录添加分页功能：后端 API `/api/deploy-logs` 支持 `page`/`page_size` 参数，Service 层返回 `{items, total, page, page_size, total_pages}`，前端支持页码按钮、上/下一页、每页条数选择(10/15/30/50)，默认15条/页
- 改动文件：`app/routers/logs.py`、`app/services/deploy_service.py`、`static/app.js`、`templates/dashboard.html`、`static/style.css`
- 侧边栏"资源监控"改为可折叠子菜单（应用资源/系统资源），修复 `toggleSubmenu` 跨菜单组误操作问题
- K8S 部署首次部署不再执行 `rollout restart`，避免双 Pod Running 被判失败
- K8S 部署添加钉钉/企微通知，bot_id 从请求传入
- 钉钉加签：`DINGTALK_SECRET` 配置项，HMAC-SHA256 加签，仅对 oapi.dingtalk.com URL 生效
- 统一通知消息模板：`[时间] 项目 tag → 状态` + `部署目标：类型[IP]` + `部署模式` + `部署镜像`，SSH 显示"单机[IP]"，Docker 显示"docker[IP]"，K8S 显示"集群[IP]"
- 删除了部署输出终端的"部署输出"文字
- 重构通知代码：`notify_deploy(db, bot_id, tag, project_key, image, status, deploy_mode, targets)` 统一消息模板，所有调用点（K8S/SSH/Docker）只传参数，不再各自拼消息
- 最终通知格式：`[时间] 项目 【变动提醒】\n部署版本：tag --> ✅/❌\n部署目标：...\n部署模式: ...\n部署镜像: ...`
- 删除调试 print，清理 k8s_deploy.py 的 datetime 导入
- `docs/architecture.md` 重命名为 `docs/用户说明.md`，`tests/` 撤销提交
- commit `bda47cf`：修复bug，优化细节，已 push gitee + github
- 系统资源监控确认：不依赖 Prometheus/Metrics，全部走 SSH + 原生命令（nproc、free、df、ps、kubectl top、docker stats 等）
- CI 面板添加 30s 自动轮询，后来改为通用面板自动刷新：每个面板右上角独立控件（下拉选间隔 + 🔄 手动刷新按钮），localStorage 持久化，用户可自由开关和调间隔
- CI tag 删除同步：决定由 PHP-CI（外部服务）负责校验，tag 删除时同步清理 ci_pipeline_tags 表。CD Service 只读展示，不做校验。
- PHP-CI Harbor 可靠性加固（三处）：container.php harborClient 加 connect_timeout=5s；HarborService::request() 加指数退避重试（4xx不重试/5xx+网络重试最多2次，200ms→400ms）；scanSync() Harbor不可达时降级放行写入（CI刚push完tag大概率存在，读取侧cleanupStaleTags兜底）；新增 HARBOR_SIMULATE_DOWN 环境变量调试开关，设1后 HarborService 直接返回 error 不发起实际请求；cleanupStaleTags() 已正确跳过不可达仓库
